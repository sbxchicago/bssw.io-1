
  $("#show_more_link").attr("href", '<%=
  show_page(request.fullpath, params[:page] ? params[:page].to_i + 1 : 2)
  %>');

<% if !(@events.next_page) %>
$("#more").remove();
$(".more-top").remove();
<% end %>


  $("#events").append("<%= escape_javascript render('events', :events => @events)  %>");


